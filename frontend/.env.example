# Next.js public env vars (inlined at build time - prefix MUST be NEXT_PUBLIC_)

# Local development:
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (set in Vercel project settings instead of committing):
# NEXT_PUBLIC_API_URL=https://api.mediassist.dpdns.org
